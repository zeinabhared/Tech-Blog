<div class="container-fluid">
  {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{post.comment}}
        </div>
    </section>
 {{/each}}
</div>