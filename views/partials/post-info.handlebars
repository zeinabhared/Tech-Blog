<div class="container-fluid">
  <header class="card">
        <div>
            <p class="text-start"></p>
            <a href="/post/{{id}}"  class="text-start">{{title}} </a></p>
        </div>
</header>

<div>
    <div>
        {{content}}
    </div>
        <div>
             {{user.username}} | 
             {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
        </div>
</div>